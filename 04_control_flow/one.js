//control flow 
if(2=="2")
        console.log("Executed");  //only compares the value;
if(2==="2") // === strict equalto !== strictly not equal to
        console.log("Typed as well as the value is also is matched")